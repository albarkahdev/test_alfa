/**
 * Divider
 *
 * @format
 * @flow
 */

import styled from 'styled-components';

import {COLORS} from '../../utils/colors';

const Divider = styled.View`
  width: 100%;
  height: 1px;
  background-color: ${COLORS.HINT_OF_PENSIVE};
`;

export default Divider;
